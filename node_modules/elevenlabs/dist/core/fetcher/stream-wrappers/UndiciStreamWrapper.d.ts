import { StreamWrapper } from "./chooseStreamWrapper";
declare type EventCallback = (data?: any) => void;
export declare class UndiciStreamWrapper implements StreamWrapper<UndiciStreamWrapper | WritableStream<Uint8Array>, Uint8Array> {
    private readableStream;
    private reader;
    private events;
    private paused;
    private resumeCallback;
    private encoding;
    constructor(readableStream: ReadableStream<Uint8Array>);
    on(event: string, callback: EventCallback): void;
    off(event: string, callback: EventCallback): void;
    pipe(dest: UndiciStreamWrapper | WritableStream<Uint8Array>): UndiciStreamWrapper | WritableStream<Uint8Array>;
    unpipe(dest: UndiciStreamWrapper | WritableStream<Uint8Array>): void;
    destroy(error?: Error): void;
    pause(): void;
    resume(): void;
    get isPaused(): boolean;
    read(): Promise<Uint8Array | undefined>;
    setEncoding(encoding: string): void;
    text(): Promise<string>;
    json<T>(): Promise<T>;
    private _write;
    private _end;
    private _error;
    private _emit;
    private _startReading;
}
export {};
